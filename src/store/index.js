import { createStore } from 'vuex'

// Create a new store instance.
const store = createStore({
  state: {
    msg: 'store running',
  },
  getters: {},
  mutations: {},
  actions: {},
})

export default store
